const BASE = 'APP_',
TOAST = BASE + 'TOAST_';

/**
 * Used to send a toast notification to the snackbar
 * @type {string}
 */
export const TOAST_ADD = TOAST + 'ADD';

/**
 * Used to send a toast notification to the snackbar
 * @type {string}
 */
export const TOAST_REMOVE = TOAST + 'REMOVE';